extends layout 

block content 
  h1= title
  div
    h3 #{post.title}
    p #{post.author.username}
    p #{post.humanDate}
    p #{post.content}
  
  form(action="" method="POST")
    input(type="hidden" value=post._id name="id")
    button Delete
    
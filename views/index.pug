extends layout

block content
  h1= title
  div
    each post in posts
      div
        h2 #{post.title}
        if user && user.isMember
          p #{post.author.username}
          p #{post.humanDate}
        p #{post.content}
